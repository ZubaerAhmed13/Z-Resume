name: quality

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  static-hardening:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Run dependency-free hardening tests
        run: python tests/test_static_hardening.py
